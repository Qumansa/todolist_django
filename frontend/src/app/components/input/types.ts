export interface InputProps {
    label: string;
    name: string;
    type: string;
    autoComplete?: string;
    placeholder?: string;
    classNameForInput?: string;
    focusOnPageLoad?: boolean;
}